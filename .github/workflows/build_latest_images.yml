name: Build Latest Images

on:
  schedule:
    - cron: '47 4 * * *'
  workflow_dispatch:

jobs:
  buid_latest_images:
    runs-on: ubuntu-latest
    if: ${{ github.repository == 'swoole/docker-swoole' }}
    strategy:
      fail-fast: true
      matrix:
        php: ["7.4", "8.0"]
        platform: [linux/386, linux/amd64, linux/arm/v6, linux/arm/v7, linux/arm64/v8, linux/ppc64le, linux/s390x]

    steps:
      -
        name: Checkout
        uses: actions/checkout@v2

      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      -
        name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      -
        name: Build and push Docker image phpswoole/swoole:php${{ matrix.php }}
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          context: .
          file: dockerfiles/latest/php${{ matrix.php }}/cli/Dockerfile
          platforms: ${{ matrix.platform }}
          push: true
          tags: phpswoole/swoole:php${{ matrix.php }}

      -
        name: Image digest of phpswoole/swoole:php${{ matrix.php }}
        run: echo ${{ steps.docker_build.outputs.digest }}

      - name: Tag latest image
        if: ${{ matrix.php }} == '8.0'
        run: |
          docker tag phpswoole/swoole:php${{ matrix.php }} phpswoole/swoole:latest
          docker push phpswoole/swoole:latest

      -
        name: Build and push Docker image phpswoole/swoole:php${{ matrix.php }}-dev
        id: docker_build_dev
        uses: docker/build-push-action@v2
        with:
          context: .
          file: dockerfiles/latest/php${{ matrix.php }}/cli/Dockerfile
          platforms: ${{ matrix.platform }}
          push: true
          tags: phpswoole/swoole:php${{ matrix.php }}-dev
          build-args: DEV_MODE=true

      -
        name: Image digest of phpswoole/swoole:php${{ matrix.php }}-dev
        run: echo ${{ steps.docker_build_dev.outputs.digest }}

      - name: Tag latest dev image
        if: ${{ matrix.php }} == '8.0'
        run: |
          docker tag phpswoole/swoole:php${{ matrix.php }}-dev phpswoole/swoole:latest-dev
          docker push phpswoole/swoole:latest-dev
